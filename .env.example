# Copy this file to .env and fill in your API keys

# Google Maps API Key (optional - only needed if implementing native maps)
# The current implementation opens Google Maps externally, so this is not required
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Firebase configuration is hardcoded in src/firebase/config.ts
# No additional environment variables needed